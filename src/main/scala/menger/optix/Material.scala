package menger.optix

import menger.common.{Material => CommonMaterial}

// Type alias for backward compatibility — Material now lives in menger.common
type Material = CommonMaterial
val Material = CommonMaterial
