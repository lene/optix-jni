# OptiX JNI Public Release Checklist (superseded)

This file is superseded — it is not referenced by any release process, and optix-jni already
releases as a standalone published Maven Central artifact (`io.github.lene:optix-jni`). Use
the workspace's `/release-checklist` skill instead; it covers all three repos' releases,
including this one, in dependency order (menger-common → optix-jni → menger).

Prior content (a pre-publication readiness checklist, last updated 2025-11-21) is preserved
in git history — see `git log -- RELEASE_CHECKLIST.md`.
